query instances($offset: Int, $limit: Int) {
  machines(offset: $offset, limit: $limit) {
    total
    offset
    limit
    results {
      id
      name
      state
      primary_ip
      firewall_enabled
      created
      updated
      brand
      memory
      disk
    }
  }
}
