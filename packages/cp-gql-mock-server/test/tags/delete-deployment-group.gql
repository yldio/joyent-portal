mutation deleteDeploymentGroup($id: ID!) {
  deleteDeploymentGroup(id: $id) {
    id
    status
    services {
      slug
      status
      instances {
        name
        status
      }
    }
  }
}
