query instance($name: String!) {
  machines(name: $name) {
    id
    name
    firewall_enabled
    firewall_rules {
      id
      enabled
      rule_str
      rule_obj
      global
      description
    }
  }
}
