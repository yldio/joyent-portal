query instance($name: String!) {
  machines(name: $name) {
    results {
      id
      name
      snapshots {
        id
        name
        state
        created
        updated
      }
    }
  }
}
