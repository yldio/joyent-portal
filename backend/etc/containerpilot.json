{
  "consul": "{{ .CONSUL }}:8500",
  "services": [
    {
      "name": "joyent-dashboard-backend",
      "port": 8000,
      "health": "/usr/bin/curl -o /dev/null --fail -s http://localhost:8000/",
      "poll": 3,
      "ttl": 10
    }
  ],
  "backends": [
  ]
}
